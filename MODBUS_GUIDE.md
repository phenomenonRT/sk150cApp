# Полное техническое руководство по протоколу Modbus RTU для ZK-SK150C

## 1. Спецификации физического интерфейса и связи
Устройство использует последовательный интерфейс TTL для обмена данными по стандартному протоколу **MODBUS-RTU**.
* **Стартовые биты:** 1 бит
* **Биты данных:** 8 бит
* **Четность:** Нет (None)
* **Стоповые биты:** 1 бит
* **Скорость передачи данных по умолчанию:** 115200 bps (настраивается в меню прибора)
* **Адрес устройства по умолчанию:** 01H (настраивается от 1 до 255)
* **Временные интервалы шины:** Пауза между фреймами должна составлять не менее 3.5 символов (при 9600 bps — более 5 мс).

---

## 2. Разделение типов управления (ОЗУ vs Энергонезависимая память)
Для предотвращения преждевременного износа встроенной флеш-памяти при автоматическом циклическом управлении от ПК или контроллера, карта регистров строго разделена на две области:

1. **Оперативное управление (ОЗУ / RAM):** Регистры в диапазоне `0000H`–`001FH`. Запись сюда изменяет текущие выходные параметры мгновенно, без задержек и **без сохранения**. После выключения питания или перезагрузки блока значения будут утеряны, а устройство вернется к предустановкам по умолчанию. Ресурс циклов перезаписи неограничен.
2. **Долговременное хранение (Флеш / Flash):** Регистры дата-групп в диапазоне `0050H` и далее. Изменения записываются напрямую в энергонезависимую память чипа. Значения сохраняются неограниченно долго после снятия основного питания.

---

## 3. Таблица оперативных регистров (Управление в реальном времени, БЕЗ сохранения)
Все данные в ячейках представляют собой 16-битные целые числа (2 байта, Big Endian).

| Адрес регистра (Hex) | Название | Описание параметров | Множитель / Младший разряд | Тип доступа |
| :--- | :--- | :--- | :--- | :--- |
| **`0000H`** | **V-SET** | **Оперативная установка выходного напряжения** | 0.01 V (12.00V = 1200 / `04B0H`) | R/W |
| `0001H` | I-SET | Оперативная установка ограничения тока | 0.001 A (2.000A = 2000 / `07D0H`) | R/W |
| `0002H` | VOUT | Текущее измеренное выходное напряжение | 0.01 V | R |
| `0003H` | IOUT | Текущий измеренный выходной ток | 0.001 A | R |
| `0004H` | POWER | Текущая выходная мощность | 0.1 W | R |
| `0005H` | UIN | Входное напряжение питания | 0.01 V | R |
| `0006H` | AH-LOW | Младшие 16 бит накопленной емкости | 1 mAh | R |
| `0007H` | AH-HIGH | Старшие 16 бит накопленной емкости | 1 mAh | R |
| `0008H` | WH-LOW | Младшие 16 бит накопленной энергии | 1 mWh | R |
| `0009H` | WH-HIGH | Старшие 16 бит накопленной энергии | 1 mWh | R |
| `000AH` | OUT_H | Время работы — часы | 1 час | R |
| `000BH` | OUT_M | Время работы — минуты | 1 минута | R |
| `000CH` | OUT_S | Время работы — секунды | 1 секунда | R |
| `000DH` | T_IN | Внутренняя температура устройства | 0.1 °C | R |
| `000EH` | T_EX | Внешняя температура (выносной датчик) | 0.1 °C | R |
| `000FH` | LOCK | Блокировка клавиатуры (`0` - разблок, `1` - блок) | Целое число | R/W |
| `0010H` | PROTECT | Текущий статус триггера защиты (0-11) | `0`: Норма; `1`:OVP; `2`:OCP; `3`:OPP; `4`:LVP; `5`:OAH; `6`:OPH; `7`:OTP; `8`:OEP; `9`:OHP; `10`:ICP; `11`:IVP | R/W |
| `0011H` | CVCC | Режим стабилизации (`0`: CV - вольты, `1`: CC - ток) | Целое число | R |
| `0012H` | ONOFF | Состояние выхода (`0`: Выход выключен, `1`: Выход включен) | Целое число | R/W |
| `0013H` | F-C | Символ единицы измерения температуры | Целое число | R |
| `0014H` | B-LED | Яркость экрана (диапазон `0`–`5`) | Целое число | R/W |
| `0015H` | SLEEP | Время автоматического отключения экрана (тайм-аут) | Минуты | R/W |
| `0016H` | MODEL | Идентификатор модели устройства | Целое число | R |
| `0017H` | VERSION | Версия встроенного микропоколения (прошивки) | Целое число | R |
| `0018H` | SLAVE-ADD | Сетевой адрес Modbus для данного устройства | Диапазон 1-255 | R/W |
| `0019H` | BAUDRATE_L | Скорость последовательной шины | Константа | R/W |
| `001AH` | T-IN-OFFSET | Калибровочное смещение внутренней температуры | 0.1 °C | R/W |
| `001BH` | T-EX-OFFSET| Калибровочное смещение внешней температуры | 0.1 °C | R/W |
| `001CH` | BUZZER | Состояние звукового излучателя (`0` - выкл, `1` - вкл) | Целое число | R/W |
| `001DH` | EXTRACT-M | Мгновенный вызов профиля данных (диапазон `0`–`10`) | Запись номера загружает профиль `M0-M10` | R/W |
| `001EH` | DEVICE | Статус периферийных узлов устройства | Целое число | R/W |
| `001FH` | V-SET (Дубль)| Дублирующий регистр оперативного вольтажа | 0.01 V | R/W |

---

## 4. Карта регистров сохранения в ПАМЯТЬ (Flash, диапазон `0050H` и далее)
Устройство поддерживает 11 независимых групп данных хранения (`M0`–`M10`), каждая из которых резервирует ровно **14 регистров (смещение 0010H)**.

### Метод расчета начального адреса группы данных:
$$	ext{Начальный адрес группы } (Hex) = 0050	ext{H} + (	ext{Номер группы} 	imes 0010	ext{H})$$

* Начальный адрес для **`M0`**: `0050H`
* Начальный адрес для **`M1`**: `0060H`
* Начальный адрес для **`M2`**: `0070H`
* Начальный адрес для **`M3`**: `0050H + 3 * 0010H = 0080H`
* Начальный адрес для **`M10`**: `0050H + 10 * 0010H = 00F0H`

### Структура смещений внутри любой группы данных (на примере группы M0):
При записи по данным адресам параметры жестко фиксируются во флеш-памяти и будут применены автоматически при вызове группы.

| Абсолютный адрес (Hex) | Смещение | Функция | Описание сохраняемых параметров | Множитель |
| :--- | :--- | :--- | :--- | :--- |
| **`0050H`** | `+0` | **V-SET** | **Сохраняемое напряжение по умолчанию для M0** | 0.01 V |
| `0051H` | `+1` | I-SET | Сохраняемый ток по умолчанию для M0 | 0.001 A |
| `0052H` | `+2` | S-LVP | Значение защиты от пониженного входного напряжения (LVP) | 0.01 V |
| `0053H` | `+3` | S-OVP | Значение защиты от превышения выходного напряжения (OVP) | 0.01 V |
| `0054H` | `+4` | S-OCP | Значение защиты от перегрузки по току (OCP) | 0.001 A |
| `0055H` | `+5` | S-OPP | Значение защиты от превышения мощности (OPP) | 0.1 W |
| `0056H` | `+6` | S-OHP_H | Лимит таймера защиты — часы | 1 час |
| `0057H` | `+7` | S-OHP_M | Лимит таймера защиты — минуты | 1 минута |
| `0058H` | `+8` | S-OAH_L | Защита по емкости OAH — младшие 16 бит | 1 mAh |
| `0059H` | `+9` | S-OAH_H | Защита по емкости OAH — старшие 16 бит | 1 mAh |
| `005AH` | `+10` | S-OWH_L | Защита по энергии OWH — младшие 16 бит | 10 mWh |
| `005BH` | `+11` | S-OWH_H | Защита по энергии OWH — старшие 16 бит | 10 mWh |
| `005CH` | `+12` | S-OTP | Уставка температурной защиты (OTP) | 0.1 °C |
| `005DH` | `+13` | S-INI | Состояние выхода по умолчанию при старте группы (`0`/`1`) | Переключатель |
| `005EH` | `+14` | S-CLOSE | Поведение ключей при срабатывании защитных триггеров | Биты маски |

---

## 5. Примеры готовых HEX пакетов управления (Адрес Slave = 01)

### Изменение напряжения без сохранения (Работа с регистром `0000H`):
* **Установить напряжение 12.00 В без записи во флеш:**
  Запрос: `01 06 00 00 04 B0 8A BE`
* **Установить напряжение 15.00 В без записи во флеш:**
  Запрос: `01 06 00 00 05 DC 99 64`
* **Установить напряжение 24.00 В без записи во флеш:**
  Запрос: `01 06 00 00 09 60 83 4E`

### Изменение напряжения с записью во флеш-память (Группа M0, регистр `0050H`):
* **Сохранить во флеш-память группы M0 базовое напряжение 12.00 В:**
  Запрос: `01 06 00 50 04 B0 89 7E`
* **Сохранить во флеш-память группы M3 базовое напряжение 5.00 В (`0050H + 3*10H = 0080H`):**
  Запрос: `01 06 00 80 01 F4 88 15`

### Оперативное управление состоянием силовой платы (Регистр `0012H`):
* **Включить выход блока питания (ON):**
  Запрос: `01 06 00 12 00 01 E8 0F`
* **Выключить выход блока питания (OFF):**
  Запрос: `01 06 00 12 00 00 29 C7`

---

## 6. Алгоритмический скрипт расчета контрольной суммы CRC16 на Python
Для интеграции команд в собственные утилиты автоматизации используйте стандартный полином Modbus RTU (`0xA001`), где младший байт суммы передается первым:

```python
def calculate_modbus_crc(data: bytes) -> bytes:
    crc = 0xFFFF
    for pos in data:
        crc ^= pos
        for _ in range(8):
            if (crc & 0x0001) != 0:
                crc >>= 1
                crc ^= 0xA001
            else:
                crc >>= 1
    # Возвращаем в формате Low Byte, High Byte
    return bytes([crc & 0xFF, (crc >> 8) & 0xFF])
```
